class WMH_HerbalistManager {
  private var cooldown: WMH_TickerGameTime;

  public function init() {
    var one_minute: int = 60;

    this.extracting_cooldown = (new WMH_TickerGameTime in this).init(one_minute * 5);
  }
}